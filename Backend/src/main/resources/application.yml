spring:
  application:
    name: Backend

  datasource:
    url: jdbc:postgresql://localhost:5432/college_db
    driver-class-name: org.postgresql.Driver
    username: sajid
    password: sajid@1584

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        globally_quoted_identifiers: true
  h2:
    console:
      enabled: true
server:
  port: 8080
logging:
  level:
    root: info
    dev:
      sajid:
        backend: debug
  file:
    name: logs/app.log
app:
  jwt-secret: 'feinjqI95B+Ai0xn4roSPgfnfI/lAvgbTpBqrcSJ+Vw='
  jwt-expiration-ms: 36000000
